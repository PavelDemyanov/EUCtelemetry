Техническое задание (ТЗ) на отрисовку дуги скорости

1. Цель работы

Разработать функцию, которая строит дугу, визуализирующую скорость в диапазоне 0 – 100 км/ч. Дуга изменяет свою длину и цвет в зависимости от значения скорости. Фон изображения отсутствует (прозрачный PNG). Дуга должна быть отражена по горизонтали.

2. Входные параметры

Функция должна принимать следующие аргументы:
	•	speed: int — текущее значение скорости в диапазоне 0 – 100 км/ч.
	•	image_size: tuple(int, int) = (800, 800) — размер изображения (по умолчанию 800x800 пикселей).
	•	arc_width: int = 20 — толщина дуги.
	•	output_path: str = "output.png" — путь для сохранения результата.

3. Правила отрисовки дуги

3.1. Геометрия дуги
	•	Дуга представляет собой часть окружности.
	•	Начальный угол: 150° (нижний правый сектор).
	•	Конечный угол: зависит от скорости и вычисляется по формуле:
￼
	•	При 0 км/ч дуга короткая (начинается с 150°).
	•	При 100 км/ч дуга достигает 390° (30° после 360°).

3.2. Отражение по горизонтали
	•	Углы дуги должны быть отражены по горизонтали.
	•	Это означает, что реальные углы будут преобразованы следующим образом:
￼
￼
	•	Таким образом:
	•	Начальный угол 150° становится 210°.
	•	Максимальный угол 390° становится -30° (эквивалентно 330°).

3.3. Изменение цвета дуги

Цвет дуги должен изменяться в зависимости от скорости:
	•	0 – 70 км/ч → переход от зелёного к жёлтому.
	•	0 км/ч → зелёный (0, 255, 0).
	•	70 км/ч → жёлтый (255, 255, 0).
	•	Градиент рассчитывается по формуле:
￼
￼
	•	70 – 85 км/ч → переход от жёлтого к красному.
	•	70 км/ч → жёлтый (255, 255, 0).
	•	85 км/ч → красный (255, 0, 0).
	•	Градиент рассчитывается по формуле:
￼
￼
	•	85 – 100 км/ч → красный цвет (255, 0, 0).
	•	Цвет не меняется.

3.4. Графическая реализация
	•	Фон отсутствует (прозрачный PNG).
	•	Дуга должна быть с закругленными концами.
	•	Толщина дуги регулируется параметром arc_width.
	•	Отрисовка дуги выполняется с помощью Python Pillow (PIL).

4. Требования к коду
	•	Язык: Python 3.
	•	Библиотеки: PIL (Pillow).
	•	Выходной формат: PNG (output.png) с прозрачным фоном.
	•	Код должен быть оптимизированным и легко редактируемым.

5. Ожидаемый результат

При вызове функции с различными значениями speed:
	•	draw_speed_arc(10) → короткая зелёная дуга.
	•	draw_speed_arc(50) → жёлто-зелёная дуга.
	•	draw_speed_arc(75) → жёлто-красная дуга.
	•	draw_speed_arc(100) → полностью красная дуга.

Готовое изображение output.png содержит только дугу без фона, с правильным отображением углов.

Готово для передачи разработчику! 🚀

Если нужны уточнения или правки — говори!